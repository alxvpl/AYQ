node_modules/
dist/
dist-test/
release/
.local-dist/

# electron-builder resources: the accepted icon artwork and the derived .ico.
# The repository root ignores build/ for upstream reasons; this package ships
# nothing from here (package.json files: dist/** and package.json only).
!build/
